{% layout settings.customer_layout %}
<article id="template" class="container clearfix">
  {% include 'breadcrumb' %}
  {% include 'switch-candy' %}
  {% unless shop.checkout.guest_login %}
    <section id="new-customer">
      <!-- Create Customer -->
      <div id="create-customer">
        <div class="template_header">
          <h2 class="title">Create Account</h2>
        </div>
        {% form 'create_customer' %}
          {{ form.errors | default_errors }}
          <div id="first_name" class="clearfix large_form">
            <label for="first_name" class="login hidden">First Name</label>
            <input type="text" value="" name="customer[first_name]" id="first_name" class="large" size="30" placeholder="First Name">
          </div>
          <div id="last_name" class="clearfix large_form">
            <label for="last_name" class="login hidden">Last Name</label>
            <input type="text" value="" name="customer[last_name]" id="last_name" class="large" size="30" placeholder="Last Name">
          </div>
          <div id="email" class="clearfix large_form">
            <label for="email" class="login hidden">Email Address</label>
            <input type="email" value="" name="customer[email]" id="email" class="large" size="30" placeholder="Email Address">
          </div>
          <div id="password" class="clearfix large_form">
            <label for="password" class="login hidden">Password</label>
            <input type="password" value="" name="customer[password]" id="password" class="large password" size="30" placeholder="Password">
          </div>
          <div class="action_bottom">
            <input class="awesome" type="submit" value="Register" />
          </div>
        {% endform %}
      </div><!-- /#create-customer -->
    </section>
  {% endunless %}
  <section id="customers" class="first uneven">
    <h2>Enter Your Email:</h2>
    {% form 'customer_login' %}
      {{ form.errors | default_errors }}
      <div id="login_email" class="clearfix large_form">
        <label for="customer_email" class="hidden">Email Address</label>
        <input type="email" value="" placeholder="Email Address" name="customer[email]" id="customer_email" class="large" />
      </div>
      {% if form.password_needed %}
        {% comment %}*** Customer Account Login ***{% endcomment %}
        <div id="login_password" class="clearfix large_form">
          <label for="customer_password" class="hidden">Password</label>
          <input type="password" placeholder="Password" value="" name="customer[password]" id="customer_password" class="large password" size="16" />
        </div>
      {% endif %}
      <div class="action_bottom">
        <input class="awesome" type="submit" value="Secure Sign In" />
        <div id="forgot_password">
          <a href="#" onclick="showRecoverPasswordForm();return false;">Don&rsquo;t know your password?</a>
        </div>
      </div>
    {% endform %}
  </section>
  {% comment %}*** Recover Password Form ***{% endcomment %}
  <section id="recover-password" class="first uneven" style='display:none'>
    <div class="template_header">
      <h2>Reset Password</h2>
      <p class="note">We will send you an email to reset your password.</p>
    </div>
    {% form 'recover_customer_password' %}
      {{ form.errors | default_errors }}
      <div id="recover_email" class="clearfix large_form">
        <label for="email" class="hidden">Email</label>
        <input type="email" value="" placeholder="Email Address" name="email" id="recover-email" class="large" />
      </div>
      <div class="action_bottom">
        <input class="awesome" type="submit" value="Submit" />
        <span class="note">or <a href="#" onclick="hideRecoverPasswordForm();return false;">Cancel</a></span>
      </div>
    {% endform %}
  </section>
  {% comment %}*** Guest Login form for shops with optional customer accounts. only displayed pre-checkout. ***{% endcomment %}
  {% if shop.checkout.guest_login %}
  <section id="guest" class="last uneven">
    <div class="template_header">
      <h2>Enter Your Email:</h2>
    </div>
    {% form 'guest_login' %}
      <div id="email" class="clearfix">
        <label for="email" class="login hidden">Email Address</label>
        <input type="email" value="" name="customer[email]" id="email" class="large" placeholder="Email Address">
      </div>
      <input class="awesome" type="submit" value="Continue with Purchase" />
      <p>You will be able to create an account later if you want.</p>
    {% endform %}
  </section>
  {% endif %}
</article>